{
  "name": "industriasp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node startup.js",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend && npm install --prefix admin"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "chalk": "^5.3.0"
  }
}